name: anime_world_v5
description: Anime streaming app shell with catalog, seasons, episodes and in-app HLS player.
publish_to: none
version: 5.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  video_player: ^2.9.2

flutter:
  uses-material-design: true
